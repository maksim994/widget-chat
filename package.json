{
  "name": "widget-chat-platform",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=@widget/shared",
    "build:shared": "npm run build --workspace=@widget/shared",
    "dev": "npm run build:shared && npm run dev --workspace=@widget/api & npm run dev --workspace=@widget/cabinet & npm run dev --workspace=@widget/embed",
    "dev:api": "npm run build:shared && npm run dev --workspace=@widget/api",
    "dev:cabinet": "npm run dev --workspace=@widget/cabinet",
    "dev:widget": "npm run dev --workspace=@widget/embed",
    "build": "npm run build --workspaces --if-present",
    "build:prod": "npm run build --workspace=@widget/shared && npm run build --workspace=@widget/embed && npm run build --workspace=@widget/api && npm run build --workspace=@widget/cabinet",
    "db:push": "npm run db:push --workspace=@widget/api",
    "db:seed": "npm run db:seed --workspace=@widget/api"
  }
}
